import type {DashboardAgentStatus} from '../../models/api'; export function AgentStatusPanel({items}:{items:DashboardAgentStatus[]}){return <section className="operator-panel"><h2>Agentenstatus</h2><div className="agent-status-list">{items.map(x=><article key={x.name}><i className={`agent-dot ${x.status}`}/><div><b>{x.name}</b><small>{x.message}</small></div><span>{x.status}</span></article>)}</div></section>}

import type {DashboardEvent} from '../../models/api'; export function NotificationPanel({items}:{items:DashboardEvent[]}){return <section className="operator-panel"><h2>Systemmeldungen</h2>{items.length?items.map(x=><article className={`notification ${x.severity}`} key={x.id}><b>{x.title}</b><p>{x.message}</p><small>{new Date(x.created_at).toLocaleString('de-DE')}</small></article>):<p className="muted">Keine neuen Systemmeldungen.</p>}</section>}

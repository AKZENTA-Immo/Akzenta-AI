import type {DashboardLead} from '../../models/api'; export function LeadPanel({items}:{items:DashboardLead[]}){return <section className="operator-panel"><h2>Neue Leads</h2>{items.length?<div className="operator-list">{items.map(x=><article key={x.lead_id}><b>{x.name||'Unbekannter Lead'}</b><span>Score {x.lead_score} · {x.status}</span><small>{x.next_action||x.lead_id}</small></article>)}</div>:<p className="muted">Keine Leads vorhanden.</p>}</section>}

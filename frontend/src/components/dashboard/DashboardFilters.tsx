export interface DashboardFilter { search:string; status:string; date_from:string; date_to:string }
export function DashboardFilters({value,onChange}:{value:DashboardFilter;onChange:(v:DashboardFilter)=>void}) { return <section className="dashboard-filters"><input aria-label="Dashboard durchsuchen" placeholder="Name, Telefon, Session, Workflow …" value={value.search} onChange={e=>onChange({...value,search:e.target.value})}/><select aria-label="Status" value={value.status} onChange={e=>onChange({...value,status:e.target.value})}><option value="">Alle Status</option><option value="active">Aktiv</option><option value="completed">Abgeschlossen</option><option value="failed">Fehlgeschlagen</option></select><input aria-label="Von" type="date" value={value.date_from} onChange={e=>onChange({...value,date_from:e.target.value})}/><input aria-label="Bis" type="date" value={value.date_to} onChange={e=>onChange({...value,date_to:e.target.value})}/></section> }

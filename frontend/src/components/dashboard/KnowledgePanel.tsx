import type {DashboardKnowledgeSource} from '../../models/api'; export function KnowledgePanel({items}:{items:DashboardKnowledgeSource[]}){return <section className="operator-panel"><h2>Wissensquellen</h2>{items.length?items.map(x=><article key={`${x.document_id}-${x.section}`}><b>{x.filename}</b><p>{x.chunk_preview}</p><small>Relevanz {Math.round(x.relevance_score*100)} % · {x.relative_path}</small></article>):<p className="muted">Keine Wissensquelle verwendet.</p>}</section>}

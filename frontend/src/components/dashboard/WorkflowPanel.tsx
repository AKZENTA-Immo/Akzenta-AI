import type {DashboardWorkflow} from '../../models/api'; export function WorkflowPanel({items}:{items:DashboardWorkflow[]}){return <section className="operator-panel"><h2>Workflow-Status</h2>{items.length?<div className="operator-list">{items.map(x=><article key={x.workflow_id}><b>{x.workflow_type}</b><span>{x.status} · {x.current_step||'Kein aktueller Schritt'}</span><small>{x.workflow_id}</small></article>)}</div>:<p className="muted">Keine Workflows vorhanden.</p>}</section>}

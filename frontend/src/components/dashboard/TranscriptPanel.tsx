import type { DashboardConversation } from '../../models/api'
export function TranscriptPanel({conversation}:{conversation:DashboardConversation|null}) { if(!conversation)return null; return <section className="operator-detail"><h3>Gespräch {conversation.session_id}</h3><p>{conversation.summary||'Keine Zusammenfassung vorhanden.'}</p><dl><div><dt>Lead Score</dt><dd>{conversation.lead_score}</dd></div><div><dt>Nächste Aktion</dt><dd>{conversation.next_action||'–'}</dd></div></dl><h4>Transkript</h4>{conversation.messages?.length?conversation.messages.map((m,i)=><p key={i}><b>{m.speaker}:</b> {m.text}</p>):<p className="muted">Kein Transkript vorhanden.</p>}</section> }
